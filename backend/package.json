{
  "name": "beacon-scholarship-backend",
  "version": "1.0.0",
  "description": "Backend server for Beacon Scholarship application",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "echo 'Backend build complete. Ensure frontend is built and copied to public/build'",
    "postinstall": "node -e \"console.log('Backend dependencies installed. Ready to serve!')\" "
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "axios": "^1.6.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
